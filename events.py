import random

class WarEvent:
    def __init__(self, name, effect, chance):
        self.name = name
        self.effect = effect
        self.chance = chance

    def trigger_event(self):
        R = random.randint(0,100)
        if R < self.chance:
            print('пока не проработана механика!!!')
            #print(f"{R}<{self.chance}")
            print(f"Событие: {self.name}!")
            print(f"Эффект: {self.effect}")
            print('пока не проработана механика!!!')
        else:
            pass
            #print(f"{R}>={self.chance}")
        
        

        

# Список событий
events = [
    WarEvent("Эпидемия среди войск", "Все юниты теряют 1 единицу здоровья.", 20),
    WarEvent("Тайный заговор", "Один случайный полководец или вассал уходит в изгнание или погибает.", 15),
    WarEvent("Моральный дух", "Все юниты получают 10% бонус к силе в следующей битве.", 25),
    WarEvent("Тимуридское вторжение", "Потеря 2 случайных юнитов, но появление новых союзников.", 10),
    WarEvent("Вражеский шпион", "Снижение эффективности осадных орудий на 20%.", 15),
    WarEvent("Заключение мира", "Останавливается несколько битв, обе стороны получают дополнительные юниты.", 5),
    WarEvent("Беспокойства среди крестьян", "Потеря 1 юнита пехоты или рыцарей.", 10),
    WarEvent("Судьбоносный выбор", "Выбор: увеличить скорость армии или повысить боевой дух.", 5),
    WarEvent("Нехватка припасов", "Все юниты теряют 1 единицу здоровья.", 10),
    WarEvent("Победа с великими потерями", "Все юниты теряют 2 единицы здоровья, но получаете 1 новый юнит.", 20)
]

# Функция для применения событий
def apply_event():
    """Выбирает случайное событие и применяет его эффект."""
    event = random.choice(events)
    event.trigger_event()

if __name__ == "__main__":
    # Пример применения событий в цикле (для каждой битвы)
    for i in range(10):
        print(f"\n=== Битва {i+1} ===")
        
        # Применение случайного события
        apply_event()